{
	"name": "nomic-bot",
	"version": "1.0.0",
	"main": "index.js",
	"license": "GPL-3.0",
	"scripts": {
		"dev": "tsc --build && node dist/index.js",
		"build": "tsc --build --verbose",
		"start": "tsc --build && pm2 start dist/index.js --name nomic-bot --interpreter node"
	},
	"dependencies": {
		"axios": "^0.26.1",
		"better-logging": "^4.7.0",
		"discord.js": "^13.6.0"
	},
	"devDependencies": {
		"pm2": "^5.2.0",
		"typescript": "^4.6.3"
	}
}
